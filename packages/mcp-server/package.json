{
  "name": "@clockchain/mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing the Clockchain network API as tools (stdio + optional HTTP)",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "clockchain-mcp": "dist/stdio.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "tsc -b && node --test"
  },
  "dependencies": {
    "@clockchain/core": "0.1.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24"
  },
  "engines": {
    "node": ">=18"
  }
}
