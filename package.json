{
  "name": "clockchain-mcp-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Clockchain MCP POC monorepo (core + MCP server)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
