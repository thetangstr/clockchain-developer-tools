# Retro — Clockchain MCP · Thu 2026-06-11 → Sat 2026-06-13

## TL;DR
In ~3 days the MCP went from a **Mac-mini interim host** to a **production-hardened,
auto-deploying GCP Cloud Run service** at `https://mcp.clockchain.network/mcp`, grew
**25 → 30 tools** (added the TSA commitment lifecycle), and gained a real engineering
spine: CI/CD with a test gate (keyless WIF), monitoring + edge protection,
bring-your-own-key, an execution-scored eval harness, and a working
feedback → backlog → ship loop.

## Timeline (what shipped)
**Thu 06-11 — interim host.** Contract-name sanitizer fix (scheduler 500 bytecode bug);
pm2 hardening; ran the MCP on the Mac mini behind a Cloudflare quick tunnel.

**Fri 06-12 — production hosting + the spine.**
- Containerized + hosted on **Cloud Run** (`mcp.clockchain.network`): external HTTPS LB,
  static IP `34.111.4.193`, Google-managed TLS, scale-to-zero→min-instances=1.
- **Auth:** token-gated + **bring-your-own-key** (`x-clockchain-*` headers → caller's
  own credits). Exec token minted.
- **CI/CD:** GitHub Actions → Cloud Run via **Workload Identity Federation** (no stored
  keys), **deploy gated on tests**.
- **Ops/security:** Cloud Armor (per-IP rate limit), uptime check + email alert,
  HTTP→HTTPS redirect, $25 budget, Secret Manager, project-scoped org-policy override.
- **Fixes:** `asset_hash` format validation; per-process log-budget (was per-request).
- **Docs:** `CLOUD-RUN.md`, `AGENTS.md`, `cicd-plan.md`, `auth-and-traffic-decision.md`,
  `agentdash-mcp-integration-brief.md`. Mac mini retired. Playground repointed to the
  hosted MCP.

**Sat 06-13 — features + eval + loop.**
- Reconciled the Linear backlog (closed 8 shipped items, updated 1).
- **TSA commitment lifecycle** (PR #45): `tsa_issue → tsa_checkpoint → tsa_attest →
  tsa_settle` + `tsa_status` (25 → **30 tools**, live in prod). Verdict from on-chain
  time vs deadline; consequence recorded, not enforced (MVP).
- **Eval harness** (PR #46, `eval/`): Layer-A perf + Layer-B execution-scored agent
  task suite. Baseline: ~4.5k-token "tax", reads p50 ~135ms; **4/5 completion**.
- The eval found a real bug → filed **AGE-180**. Loop closed.

## By the numbers
- 15 commits to `main`, 2 PRs (both gated by CI), **25 → 30 tools**.
- Linear: closed AGE-145/176/179 + AGE-158/164–168 (8), filed AGE-179/180, updated AGE-177.
- Eval baseline: Layer-A ~4,474 tok schema / reads p50 ~135ms / p99 ~350ms; Layer-B
  4/5 completion, 4/5 tool-selection, ~23k tok/task.

## What went well
- **Corrected the hosting plan fast** — abandoned the Mac-mini + tunnel + reverse-proxy
  plumbing for Cloud Run the moment it got silly; cleaner and more robust.
- **Chain-authoritative, execution-based verification** gave us an eval with *objective*
  scoring (no LLM judge) — a genuine differentiator.
- **The backlog loop works end-to-end**, demonstrated live (feedback → Linear → ship),
  including a bug the eval generated itself.
- Caught + fixed real defects: `asset_hash` validation, the budget-per-request bug, the
  `get_block` key-scope (surfaced, owned by D4).

## What went wrong (and the fix, now encoded)
- **Shipped untested code** (`asset_hash` + budget) verified only by live curl → **CI red
  on `main` for ~a day**, hidden because deploy and CI ran independently. → **Deploy gate
  (`deploy needs test`)** + `AGENTS.md` rule *"run `npm test` before push."*
- **Two agents in one working tree** (an untracked `packages/verify` WIP) broke the local
  root build and risked cross-contamination. → `AGENTS.md` *"one agent per worktree"* +
  surgical, explicit-path commits.
- **`MCP_LOG_BUDGET` was per-request** (never actually capped). → fixed to a process-wide
  shared budget; BYO requests bypass it.
- **Deploying from a laptop.** → replaced by keyless WIF CI/CD.

## Open / next
**D4-owned:** OAuth for the Cowork cloud connector (Hydra-delegates-to-identity);
`get_block`/`/api/time/*` key scope (AGE-150/171); the `create_schedule` signature spec
(AGE-157); ERC-8004 resolution (AGE-151); review the `allUsers` org-policy override.
**Backlog:** AGE-180 (`log_action` content-hash → eval 5/5); AGE-177 (playground thin MCP
client); AGE-169 (playground CI/CD); per-tenant metering / BYO-by-default.

## Artifacts
Endpoint `mcp.clockchain.network` (30 tools). Repo `clockchain-developer-tools`:
`README` · `INSTALL` · `CLOUD-RUN` · `cicd-plan` · `AGENTS` · `auth-and-traffic-decision`
· `agentdash-mcp-integration-brief` · `eval/`. Backlog: Linear **MCP Playground**.
